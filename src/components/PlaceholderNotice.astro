---
import { ui } from '../i18n/ui';
import type { Locale } from '../i18n/routes';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
---
<p class="meta">{ui.placeholderNotice[locale]}</p>
